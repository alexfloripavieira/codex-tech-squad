# Codex Tech Squad Plugin

This is the plugin payload loaded by Codex.

It provides skills and role prompts. It does not define new native Codex
subagent types. Specialists are prompt contracts mapped onto native Codex
roles:

- `explorer`
- `worker`
- `default`

See `references/agent-orchestration.md` for the runtime model.
